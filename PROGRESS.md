# beatroot Implementation Progress

- Task 1 contracts: 2026-08-30 00:56 — contracts defined, 3 tests passing
- Task 2 catalog: 2026-08-30 01:03 — ~120 ingredients, ~100 recipes, transitive tags derived at seed
- Task 3 T0 constraints: 2026-08-30 01:09 — uncheckable-hard treated as illegal
- Task 8 hybrid retrieval: 2026-08-30 02:10 — FTS5 + dense + RRF, filter-before-rank verified
- Task 9 FEATURE 2 trust+escalation: 2026-08-30 02:02 — 75/25 composite, weak-signal veto, named failing signal, incidents logged
- Task 10 skills registry: 2026-08-30 — 6 skills authored (tier+llm_permitted frontmatter matches spec §7 table), skills-lock.json generated, drift detection verified live
- Task 11 langgraph: 2026-08-30 — StateGraph (feasibility/retrieve/score/trust/explain/verify + 3 terminals), SqliteSaver checkpoints, interrupt_before=["commit"] gated on medical grey-band trust, drift ledger, audit log
- Task 24 quality gates: 2026-08-30 — ruff clean, mypy --strict clean (46 files), 92.32% coverage (>80% required), 221 passed/5 skipped; eval step deferred (no `beatroot eval` command yet — lands in Tasks 13-15, after this task in execution order)
- Task 13 golden dataset + system eval: 2026-08-30 — 26 golden cases across 6 adversarial families + religious, independent hard_constraint verifier (never calls is_legal), refusal_correctness verifier catches both under- and over-refusal, `python -m beatroot.eval.runners.system` runnable directly; A5_escalation_correctness FAILS (0.0 vs 0.90 threshold) — unknown_ingredient cases reveal that exclude_tag/exclude_ingredient constraints referencing a tag or id absent from the catalog are silently treated as always-satisfied instead of escalating; genuine finding, not softened; 255 passed/5 skipped, ruff+mypy clean
- Task 13 fix round — A5 vocabulary gap closed: 2026-08-30 — t0_invariants/vocabulary.py's unknown_vocabulary() validates exclude_tag/exclude_ingredient constraint values against the catalog's real tag/ingredient vocabulary (synonyms resolved via trusted.canonical) before feasibility runs; agent/nodes.py's feasibility node escalates with reason="unknown_ingredient" at zero token cost, agent/graph.py routes feasibility straight to escalate. Fixed grey-band test fixture (tests/agent/conftest.py) which had unknowingly relied on the same silent-pass bug. Re-ran system eval: A5_escalation_correctness now 1.000/0.9 PASS, A1-A4/A6 unchanged at 1.000, overall PASS, exit 0. 270 passed/5 skipped, ruff+mypy clean, 91.83% coverage.
- Task 14 synth + components: 2026-08-30 — free oracle verified exact (brute-force check_recipe recomputation matches oracle_valid_ids on every generated case), generate_profiles/generate_adversarial reuse real catalog synonym pairs, run_components reuses the Container's once-built vector_store; retrieval recall@5 0.603 (honest, token-hashing offline embedding), retrieval_leakage 0 (independently verified via eval.verifiers.hard_constraint, never is_legal), feasibility_accuracy 1.000, nutrition_exact_match 1.000.
- Task 15 calibration: 2026-08-30 — expected_calibration_error/reliability_curve implemented and unit-tested (overconfident case ECE≈0.45, empty input 0.0, contiguous bins covering [0,1]); real-system sweep over 200 synthetic profiles (90 COMMITs) gives ECE 0.125 — offline trust.composite is systematically underconfident (mean 0.875, actual accuracy 1.000) because model_self_assessment is pinned at the offline stub's constant 0.5, so this run validates the deterministic 75% of the score but says nothing about whether the model's own confidence is calibrated.
- Task 16 healing loop: 2026-08-30 — heal/cluster.py groups incidents by kind+digit-stripped detail; heal/proposals.py turns clusters into Proposals: every cluster (even a singleton) yields an auto_applied eval_case under generated/, loadable by eval.runners.system.load_cases/run_system; clusters at/above healing.rule_proposal_min_cluster additionally yield NEVER-auto_applied threshold/meta_tag proposals written under proposals/ — no code path opens config/beatroot.yaml or eval/thresholds.yaml for writing (test_propose_never_modifies_config_or_data asserts byte-identical + mtime-identical). heal/__main__.py gives `python -m beatroot.heal` a runnable entry point (cli/main.py Typer wiring deferred — concurrent work owns that file). Demonstrated end to end offline: system eval produced 11 real incidents (infeasible x5, unknown_ingredient x3, escalation x3) -> 7 clusters -> 8 proposals (7 auto eval_case, 1 human-gated meta_tag for the repeated infeasible cluster); one generated case verified to load and PASS through the real run_system. 310 passed/5 skipped, ruff+mypy clean, 89.41% coverage.
- Task 17 preference memory: 2026-08-30 04:42 — PreferenceMemory (store/preferences.py) is a per-tag EMA affinity, alpha from settings.preferences.ema_alpha, sharing the Container's db_lock like AuditLog/IncidentLog/caches; wired into build_container (Deps.preferences and Container.preferences, replacing the placeholder None) and already consumed by retrieve_node's affinity= arg. POST /feedback records acceptance/rejection and returns the updated affinity. Safety property proven directly: test_affinity_cannot_promote_an_illegal_candidate gives a MEDICAL-excluded tag affinity=+1.0 and shows zero results carry it — affinity only reorders retrieve()'s already-legal fused set, never adds to it. End-to-end demo (offline): starting affinity {} for a fresh profile, 20 rejections of rec_dal_makhani's tags + 20 acceptances of rec_shrimp_tacos's tag moved dal_makhani from fused rank #1 to #7, shrimp_tacos from #40 to #16, and flipped the actual /recommend commit from rec_dal_makhani to rec_panna_cotta on the identical profile/query — the loop measurably changes behaviour, not just records feedback. 334 passed/5 skipped, ruff+mypy clean, 86.89% coverage.
- Task 14+15 fix round (review response): 2026-08-30 — replaced the circular check_recipe-based oracle with an independent, from-scratch re-implementation (eval/synth/profiles._oracle_valid_ids) that never imports t0_invariants; proven capable of failing (inverted _exclude_tag in constraints.py -> cross-check test failed as expected, reverted). Added settings.synth.infeasible_fraction (0.25) + _force_infeasible_constraints so generated profiles are genuinely ~22-29% infeasible across seeds 0/2/3 (was 0-1.5%). Softened independence claims in hard_constraint.py and the new oracle's docstrings (call-graph independent, not assumption independent — both still read recipe.tags). ECE now prints its COMMIT-pair denominator inline. Re-run: components — recall@5 0.548, leakage 0, feasibility_accuracy 1.000 (now a real cross-check), nutrition 1.000; calibration — ECE 0.125 over 71 COMMIT-only pairs (down from 90, since ~25% of profiles now correctly refuse). 339 collected, all pass, ruff+mypy clean, coverage 89.63%.
- Task 14+15 second fix round (Important 3 came back): 2026-08-30 — replaced the "stack exclude_tag until empty" infeasibility mechanism (mean 13.4/16 tags excluded, a degenerate exclude-everything case) with rejection sampling over four small (2-4 constraint), individually-plausible conflict templates derived from the catalog's own cost/protein/prep distributions (tight budget vs protein floor, short prep vs 1-2 tag exclusions, protein floor vs one tag exclusion, 2-3 jointly-empty tag exclusions), each checked against the independent oracle and capped at excluding no more than 40% of the tag vocabulary. Measured: seeds 0/2/3 give 24.0%/27.0%/28.5% infeasible (in target band), mean constraint count ~2.1 (max 4), mean tags excluded 0.17-0.25 (max 3) — down from a mean of 13.4. Two new regression-guard tests assert this stays true. Re-run: components — recall@5 0.640, leakage 0, feasibility_accuracy 1.000, nutrition 1.000; calibration — ECE 0.125 over 80 COMMIT-only pairs (commit rate 40.0%, in the same neighbourhood as before). ruff+mypy clean, 89.84% coverage.
- Task 18 dashboard: 2026-08-30 05:01 — src/beatroot/web/index.html (468 lines, single self-contained file, no CDN/external fonts/build step), GET / added to api/main.py via FileResponse. All four terminal states (COMMIT/NEGOTIATE/ESCALATE/PENDING_REVIEW) verified end to end against the real API: COMMIT and NEGOTIATE (with a medical constraint locked, unclickable, struck-through) over TestClient against the real ~100-recipe catalog; ESCALATE (unknown_ingredient) same; PENDING_REVIEW + both resume(approved=True)->COMMIT and resume(approved=False)->ESCALATE against the grey-band fixture from tests/agent/conftest.py (same pattern as tests/api/test_resume.py). Trust bars (composite/catalog_coverage/constraint_completeness/model_self_assessment) with 0.55 refusal-threshold line and 75/25 deterministic/model labelling confirmed against real TrustReport payloads. /feedback affinity and /incidents feed also exercised live. tests/api/test_index.py added (4 tests: serves+no external URLs including bare http/https scan, all four state names present, untrusted-input label present, locked-never-clickable copy present). 346 passed/5 skipped, ruff+mypy clean, coverage 88.74%.
- Task 19 observability: 2026-08-30 05:09 — obs/tracing.py registers litellm.success_callback/failure_callback ("langfuse") only when settings.obs.langfuse_enabled (both LANGFUSE_PUBLIC_KEY+LANGFUSE_SECRET_KEY set, read via new Settings.obs computed property, bare env names like qdrant_url), idempotent via module-level _CONFIGURED flag; obs/logging.py gives every log line a JSONFormatter (ts/level/logger/message/request_id/profile_id + extras) on the root logger's stderr handler, bind_request() context-manager binds request_id/profile_id via contextvars (nested-safe, no leak across requests), any extra field named api_key/authorization/secret_key/password/token renders [redacted] never the real value; obs/cost.py CostLedger aggregates per-stage USD + tokens + tokens_saved across plans with derivable total_usd/per_plan_usd. Wired into container.build_container() (both CLI Typer commands and the API lifespan already call this once) rather than editing api/main.py, which a concurrent agent may be touching. uv add --optional obs langfuse>=2.50. 14 new obs tests + 3 new settings tests, 353 passed/5 skipped total, ruff+mypy clean, coverage 90.21%. Demonstrated offline: (a) JSON log line with request_id/profile_id bound via bind_request; (b) extra={"api_key": "sk-live-..."} renders [redacted], raw value absent from output; (c) configure_observability() called twice leaves litellm.success_callback.count("langfuse")==1; (d) env -i (fully stripped environment) run of configure_logging()+configure_observability() x2 completes with no error.
- Task 20 docker: 2026-08-30 05:10 — Dockerfile (non-root, port 7860, HEALTHCHECK, --extra qdrant so the compose-wired Qdrant path actually has qdrant-client installed), .dockerignore, docker-compose.yml extended (env_file .env made 'required: false' so a fresh clone with no .env still starts), README.md HF Space frontmatter added (body is Task 21's). Docker daemon (colima/VZ) unavailable in this sandbox — could not run docker build/run; verified equivalent claim at the application layer instead: 'uv run uvicorn ...' with zero BEATROOT_* env vars and no .env present served GET /health 200 (provider=azure label, no network call, no credential error) and again with BEATROOT_OFFLINE=1 (provider=echo). 'docker compose config' resolved cleanly with no .env in the directory. qdrant-client confirmed present in uv.lock under the qdrant extra. Full suite still green: 363 passed/5 skipped, ruff+mypy clean, 90.21% coverage.
- Task 23 async explain: 2026-08-30 — prose off the critical path. agent/async_explain.py's ExplanationQueue (ThreadPoolExecutor(max_workers=4), docstring documents Celery/RQ/Cloud Tasks as the real production shape behind the same interface) is opt-in via settings.async_explanation (default False, so every existing caller — CLI, eval runners, golden cases — is unaffected). explain_node submits recipe/nutrition/check and returns immediately with explanation=""; verify_node skips the drift check on an empty explanation (constraint recheck still runs unconditionally). POST /recommend returns a top-level thread_id plus explanation:null/explanation_status:pending when deferred; GET /recommend/{id}/explanation polls it (404 when async mode is off). obs.logging.bind_request now wraps the whole request so ExplanationQueue.submit() can capture+rebind contextvars into the worker thread (proven with a dedicated test reading _request_id.get() from inside the background completion). web/index.html renders the card immediately and shimmer-polls the prose cell. A failed explanation is caught, marked failed, and never invalidates the recommendation. Measured p50/p95 for POST /recommend (TestClient, n=60): offline stub sync 10.41/12.43ms vs deferred 10.45/12.03ms (negligible — the offline "model" is a hash, nothing to move off the path); simulated 700ms real-provider latency sync 732/741ms vs deferred 10.0/15.4ms (the actual claim, honestly labeled as simulated). System eval: all six axes PASS unchanged (default sync path). 405 passed/5 skipped, ruff+mypy clean, coverage 91.32%.
